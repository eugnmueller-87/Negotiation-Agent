# Runtime dependencies for the DEPLOYED Peitho v2 backend (Railway/Nixpacks).
#
# Named requirements.txt (not -deploy) because Nixpacks stages this exact filename
# into the install layer before the rest of the source is copied. The container
# runs uvicorn against src/ on PYTHONPATH (see nixpacks.toml) rather than installing
# the package — sidesteps the src-layout wheel build. Keep in sync with the [web]
# extra in pyproject.toml. This file does NOT drive local dev or CI (those use
# `pip install -e '.[dev]'` / `'.[web]'` from pyproject.toml).
pydantic>=2.6,<3
fastapi>=0.110
uvicorn>=0.29
anthropic>=0.40
# Contract file → text (the /extract-text endpoint): pure-Python PDF text-layer
# extraction (no system binary) + Word .docx; python-multipart lets FastAPI accept
# the upload. Text-layer only — a scanned PDF is warned, not OCR'd.
pypdf>=4.0
python-docx>=1.1
python-multipart>=0.0.9
# Contract PDF → stable, deep-linkable text anchors (negotiation_agent.anchor): PyMuPDF
# gives per-page geometric text blocks so every finding's quote can be verified back to a
# specific block. Single wheel, no system binary, Nixpacks-clean.
pymupdf>=1.24
